{
  "name": "typed-board",
  "packageManager": "yarn@4.0.0-rc.26",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --topological-dev -pv run build"
  }
}
